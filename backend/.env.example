# Copy to .env for local development (docker compose sets these itself)
DATABASE_URL=postgresql://forum:forum@localhost:5432/forum
JWT_SECRET=dev-secret-change-me
# OAuth 2.0 Web Client ID from Google Cloud Console (APIs & Services > Credentials)
GOOGLE_CLIENT_ID=
# Comma-separated emails that get the ADMIN role on sign-in
ADMIN_EMAILS=vinay.menon2707@gmail.com
PORT=4000

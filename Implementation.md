I have added the smtp, now when signing in, it should send the otp via this mail.

Also for the weekly digest, use this mail.

After signing in, it automatically goes to create Account? If yes why are we again taking the username and password? Instead of that, lets take in the input on which topics they are interested out of the 10 topics so that we can send them a mail each time a question on that domain is posted. Then further they should have an option to edit their favoured domain in their profile screen. This is for the mail notification. If they want mail notification for every new question posted, they can enable it in the settings.

This SMTP mail should be used for all kind of notifications, in the marketplace domain, in the mentorship, in the job section etc. I want it to be implemented in all the places where a mail is sent.

Also go through the url http://localhost:3000/ , and tell me where is the calm an soothing moving animations in the background which I had asked. Enable it.